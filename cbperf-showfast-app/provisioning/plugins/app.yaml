apiVersion: 1

apps:
  - type: 'cbperf-showfast-app'
    org_id: 1
    disabled: false
    jsonData:
      cbConnString: ${CB_CONN_STRING}
      cbUsername: ${CB_USERNAME}
    secureJsonData:
      cbPassword: ${CB_PASSWORD}
