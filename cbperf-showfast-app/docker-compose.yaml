services:
  grafana:
    extends:
      file: .config/docker-compose-base.yaml
      service: grafana
    env_file:
      - .env

networks:
  default:
    ipam:
      config:
        - subnet: 172.30.0.0/16
